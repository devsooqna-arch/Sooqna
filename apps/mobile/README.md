# Sooqna Mobile (Placeholder)

This folder is intentionally kept as a clean placeholder for the future React Native app.

## Planned Architecture

- Authentication: Firebase Auth (same as web)
- Business/data source: `backend/sooqna-backend` REST API only
- No Firestore usage in product data flows
- No Firebase Functions / Firebase Storage dependency in product flow

## TODO (when mobile build starts)

1. Initialize React Native or Expo project structure.
2. Add auth session flow with Firebase ID token retrieval.
3. Add shared API client targeting `NEXT_PUBLIC_BACKEND_API_BASE_URL` equivalent for mobile.
4. Implement listings, favorites, messages, and profile features via backend APIs.

